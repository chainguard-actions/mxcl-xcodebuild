name: Test sanitizer input
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - id: action
        uses: .
        with:
          sanitizer: thread
          action: none
      - name: Assert action completed
        run: |
          echo "PASS: action completed with sanitizer input"
