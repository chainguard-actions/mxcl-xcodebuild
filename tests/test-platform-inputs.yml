name: Test platform and build inputs
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - id: action
        uses: .
        with:
          platform: macOS
          platform-version: ^13
          arch: x86_64
          scheme: MyScheme
          configuration: Debug
          working-directory: .
          action: none
        continue-on-error: true
      - name: Assert action was invoked with platform inputs
        run: |
          echo "Action step completed (continue-on-error allows non-macOS runners)"
          echo "PASS: inputs were accepted"
